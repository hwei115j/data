#include<stdio.h>
#include"list.h"

int main()
{
	list_t linklist;
	init(&linklist);
/*	int n;
	list_t linklist;

	init(&linklist);
	while(scanf("%d", &n) != EOF)
		switch(n)
		{
		case 1:
			
		case 2:
		case 3:
		case 4:
		case 5:
		case 0:

		}*/
}
