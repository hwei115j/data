#define MAX 10000
#define LIM 938475

#define IN(n, _c)  do{for(_c = 0; scanf("%d", &n[_c]) != EOF; _c++);}while(0)
#define SWAP(a, b) do{uint32_t _swap = a; a = b; b = _swap;}while(0)
#define OUT(n, m) do{for(int i = 0; i < m; i++) printf("%d\n", n[i]);}while(0)
